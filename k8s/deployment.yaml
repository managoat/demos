apiVersion: apps/v1
kind: Deployment
metadata:
  name: briefing-room
  namespace: briefing-room
  labels:
    app: briefing-room
spec:
  replicas: 2
  selector:
    matchLabels:
      app: briefing-room
  template:
    metadata:
      labels:
        app: briefing-room
    spec:
      containers:
        - name: web
          # Pinned to an immutable sha tag by .github/workflows/build.yml.
          image: ghcr.io/managoat/briefing-room:sha-ec80b8205c62107bf7a95d168d3691a6df88db56
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            requests: { cpu: 20m, memory: 96Mi }
            limits: { memory: 256Mi }
