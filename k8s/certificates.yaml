apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dns-desk-tls
  namespace: dns-desk
spec:
  secretName: dns-desk-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - dns-desk.demo.managoat.com
