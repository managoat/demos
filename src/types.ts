/** The API's types come from the SDK, which generates them from Fountain's OpenAPI document. */
export type { Block, ConversationRecord as Conversation, LogEvent, Turn } from "@agentshit/fountain-sdk";
