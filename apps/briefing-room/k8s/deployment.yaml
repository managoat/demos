apiVersion: apps/v1
kind: Deployment
metadata:
  name: briefing-room
  namespace: briefing-room
  labels:
    app: briefing-room
spec:
  replicas: 2
  selector:
    matchLabels:
      app: briefing-room
  template:
    metadata:
      labels:
        app: briefing-room
    spec:
      containers:
        - name: web
          # Pinned to an immutable sha tag by .github/workflows/build.yml.
          image: ghcr.io/managoat/briefing-room:sha-7894ffb3d33bd59221f3a6dd4e27c762a1cbc138
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /healthz, port: 8080 }
            initialDelaySeconds: 5
            periodSeconds: 20
          resources:
            requests: { cpu: 20m, memory: 96Mi }
            limits: { memory: 256Mi }
