apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paddock-tls
  namespace: paddock
spec:
  secretName: paddock-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - paddock.demo.managoat.com
