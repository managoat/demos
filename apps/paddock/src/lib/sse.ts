/**
 * The suite's SSE reader — see `@managoat/fountain-app/sse`.
 *
 * Kept as a file here so every `import … from "./sse"` in this app reads the
 * same as it did when the library was a copy in each repository.
 */
export * from "@managoat/fountain-app/sse";
