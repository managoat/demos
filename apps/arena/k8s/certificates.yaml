apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: arena-tls
  namespace: arena
spec:
  secretName: arena-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - arena.demo.managoat.com
