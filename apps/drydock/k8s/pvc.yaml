apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drydock-data
  namespace: drydock
  labels:
    app: drydock
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
