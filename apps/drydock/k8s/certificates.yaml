apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: drydock-tls
  namespace: drydock
spec:
  secretName: drydock-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - drydock.demo.managoat.com
